原始提示词

我准备做一个项目 ,名字鉴枢, 他是一个集成测试的项目 ,主要功能是 用户可以导入 swagger ,然后用户提供测试用例 .可以直接生产测试工作流,核心功能就是这个周边如下,提供简单的用户体系,顶层集合为测试集,swagger 是导入到测试集内部 ,同一个测试集内部 swagger 可以多次导入,每一个接口为一个测试单元,接口导入不能丢失信息,swagger 上的接口相关的信息都要T保留, 基于每个接口的测试单元,可以生成测试流,生成是要基于 LLM 来生成,llm 将测试集下的所有的测试单元,+ 现有的测试流+用户的用例 提交给 LLM, LLM 来进行任务测试流创建,流上的每一个执行单元 包括启动,api执行,断言,循环,try-catch,共享缓存,输入输出适配器等执行节点,执行单元组成的流是树形结构,一个流只能有一颗树,一个 start节点, agent 需要提供一些工具给 llm, 比如:获取所有执行单元描述数据,筛选执行单元,获取当前流,创建节点,更新节点,删除节点,链接节点,流校验等, 每个节点上都有输入输出, 上级节点的输出一定要满足下游节点的输入,如果满足不了需要补充一个适配节点来转换参数,每次调用工具如不成功都要告知 llm 正确的格式,让 llm 重试,每次提交创建 可进行 50 轮工具调用, 超过需要停止并提示用户,每一轮的工具执行输出需要推送给用户的对话框, llm 的 provider 可管理,为用户维度,支持兼容 openai 的协议, 测试集可以添加用户,被添加用户有 只读和编辑两个权限, 用户的对话框在测试流的编辑页面,要基于测试流存储历史 session,方便用户持续修改,用户可以在对话框勾选特定执行单元进行向 llm 修改,测试流在每次编辑都是生成草稿,只有保存启用以后才能长效运行,草稿期间可以试运行.试运行的结果能在执行单元上显现出来,方便用户知道哪一步错了.可以是绿勾或者红叉,试运行以后点击绿勾或者红叉,可以看到具体的输入输出,同时可以查看该流的历史执行日志,点开每一条历史执行日志的时候 ,最好能够复原出这条流原本的样子,流每次变更会有历史版本 ,日志也和历史版本关联着. 创建流的过程 是一个固定工作流,一旦用户提示词意图命中,就要严格按照顺序执行.测试流可以配置定时任务,定时任务默认用 gocron, 定时能力用插件方式实现,后续可更换为外部定时服务. 这个项目前后端在一起前端放到 web 目录，用 react, 流除了 llm 生成也是可以人工编辑的.可以编辑执行节点链接,链接时要进行参数校验,最好是链接前就让 llm 知道要链接节点的输入输出, 如果链接校验失败了可以告知 llm,llm 可以选择调整参数或者在他们中间加参数适配器节点,每个节点上面都可以看到输入输出格式,断言配置等信息,也可手动编辑.流上的每个执行节点的坐标信息最好是树形分布, 共享缓存节点不参与连线,就用一个 map 实现就行,一般可以用来存放 token 信息,或者全局信息,比如 A——>B->C. c 也需要 A 的输出产物,就可以把 A 的产物设置到共享缓存节点, 哦这里需一个设置共享缓存的节点,当需要设置共享缓存时可以链接一个这样的节点,这样的节点,输出就是全部缓存,及 设置任何一个都输出共享缓存中的全部,如需获取共享缓冲的数据 在输入参数中设置指定的 key 就可以获取共享缓存数据, 后端语言 go,gorm sqlite(默认)/mysql/pgsql. 还有一点需要提示 ,如果用户的 swagger 不标准不符合预期需要用户确认,或者 llm 的返回不确定,需要让 llm按照标准格式重新生成,尽量避免 agent 用代码兼容.⃣️


T1 流里面直接冗余 schema 吧 ,因为执行单元的参数里有可能会有 api 以外的东西,可以用户自己加参数或者取共享缓存 ,T2 那就 JsonData 吧 T3 api 的 host 在测试集上配置, start 节点可以放起始参数,比如账号密码, 密码可以提供一些常用的加密套件. T4 只停该分支, try-catch 应该是两个节点 try 和 catch ,如果只有 try 就是所有子树,有 catch 就是中间的那一段, loop 就是要输入数组迭代子树 T5 按照 method+path 去重斜杠转中横杆链接. 如果有删除 不要直接删,可以标记为已删除, T6 swagger 导入不涉及 llm ,是标准导入 ,比如 swagger 定义的是 apikey 认证,但是实际看到 api 上又是 token 认证,这种在生成流的时候 要提示用户 让用户判断采用哪种. swagger 导入 模块儿信息 要标记 tag ,可以通过  tag 去查询接口,或者根据名字 或者 path 匹配